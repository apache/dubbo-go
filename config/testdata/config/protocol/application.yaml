dubbo:
  registries:
    nacos:
      timeout: 5s
      group: dev
      address: nacos://127.0.0.1:8848
  protocols:
    dubbo:
      name: dubbo
      port: 20000
      max-server-send-msg-size: 4mb
      max-server-recv-msg-size: 4mb