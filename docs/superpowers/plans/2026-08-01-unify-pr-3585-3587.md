# PR #3587 统一 #3585/#3587 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将 `develop@a62a44a266c63c00e9d22a2c018b4fdccae1dab1` 合入 PR #3587，并在不恢复内嵌生成文件的前提下补齐稳定依赖、fresh scaffold 编译、EOF 和 CI 回归。

**架构：** 合并冲突先全部收敛到 #3585 已进入 develop 的脚手架树，使合并 commit 的代码树相对 develop 只多设计/计划文档；随后用 TDD 分别加入稳定模块版本、真实 `make test` 生成项目验证和包装 EOF 处理。E2E 测试通过显式环境变量启用，普通单测不要求本地安装 protoc 工具链，GitHub Linux CI 则必须安装并执行完整链路。

**技术栈：** Go 1.25、GNU Make、Protocol Buffers `protoc`、`protoc-gen-go` v1.34.2、仓库内 `protoc-gen-go-triple`、GitHub Actions、PowerShell 与 WSL/Linux。

---

## 文件职责与变更结构

- `docs/superpowers/specs/2026-08-01-unify-pr-3585-3587-design.md`：已经批准的设计约束，不再修改其语义。
- `docs/superpowers/plans/2026-08-01-unify-pr-3585-3587.md`：本实施计划。
- `tools/dubbogo-cli/cmd/gen_test.go`：generator/golden 一致性、旧 logger、稳定版本、fresh scaffold E2E 测试入口。
- `tools/dubbogo-cli/generator/application/gomod.go`：newApp 的最小 `go.mod` 模板。
- `tools/dubbogo-cli/generator/sample/mod.go`：newDemo 的最小 `go.mod` 模板。
- `tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.mod`：newApp golden 输出。
- `tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.mod`：newDemo golden 输出。
- `tools/dubbogo-cli/generator/application/makefile.go`：newApp 的公开 Makefile 模板。
- `tools/dubbogo-cli/generator/sample/makefile.go`：newDemo 的公开 Makefile 模板。
- `tools/dubbogo-cli/cmd/testGenCode/template/newApp/Makefile`：newApp Makefile golden 输出。
- `tools/dubbogo-cli/cmd/testGenCode/template/newDemo/Makefile`：newDemo Makefile golden 输出。
- `tools/dubbogo-cli/generator/application/pkg.go`：newApp 服务实现模板。
- `tools/dubbogo-cli/generator/sample/gen_server.go`：newDemo 服务端实现模板。
- `tools/dubbogo-cli/cmd/testGenCode/template/newApp/pkg/service/service.go`：newApp 服务实现 golden 输出。
- `tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-server/cmd/server.go`：newDemo 服务端 golden 输出。
- `tools/dubbogo-cli/Makefile`：本模块普通测试和显式 E2E target。
- `.github/workflows/github-actions.yml`：Linux CI 安装协议生成工具链并运行 E2E。

### 任务 1：合并 develop，并将冲突树收敛到 #3585 架构

**文件：**
- 保留 develop：`tools/dubbogo-cli/README.md`
- 保留 develop：`tools/dubbogo-cli/README_CN.md`
- 保留 develop：`tools/dubbogo-cli/cmd/gen_test.go`
- 保留 develop：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/cmd/app.go`
- 保留 develop：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.mod`
- 保留 develop：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/pkg/service/service.go`
- 保留 develop：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-client/cmd/client.go`
- 保留 develop：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-server/cmd/server.go`
- 保留 develop：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.mod`
- 保留 develop：`tools/dubbogo-cli/generator/application/api.go`
- 保留 develop：`tools/dubbogo-cli/generator/application/cmd.go`
- 保留 develop：`tools/dubbogo-cli/generator/application/gomod.go`
- 保留 develop：`tools/dubbogo-cli/generator/application/pkg.go`
- 保留 develop：`tools/dubbogo-cli/generator/sample/gen_client.go`
- 保留 develop：`tools/dubbogo-cli/generator/sample/gen_server.go`
- 保留 develop：`tools/dubbogo-cli/generator/sample/mod.go`
- 删除：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/api/api.triple.go`
- 删除：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/api/samples_api.triple.go`
- 删除：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.sum`
- 删除：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.sum`
- 删除：`tools/dubbogo-cli/generator/internal/scaffold/gosum.go`
- 删除：`tools/dubbogo-cli/generator/sample/api_tripe.go`

- [ ] **步骤 1：重新固定远端 Head 和 develop**

运行：

```powershell
gh pr view 3587 -R apache/dubbo-go --json state,baseRefName,baseRefOid,headRefName,headRefOid
git ls-remote origin refs/heads/develop refs/heads/codex/fix-cli-template-logger
```

预期：PR 仍为 `OPEN`，PR Head 仍为 `23e50e5152c58d197317351dc835865ce26242a1`；远端 develop 为 `a62a44a266c63c00e9d22a2c018b4fdccae1dab1`。

- [ ] **步骤 2：获取最新远端 refs**

运行：

```powershell
git fetch --no-tags origin `
  refs/heads/develop:refs/remotes/origin/develop `
  refs/heads/codex/fix-cli-template-logger:refs/remotes/origin/codex/fix-cli-template-logger
```

预期：本地 `origin/develop` 与 `origin/codex/fix-cli-template-logger` 分别等于上一步固定的 OID。

- [ ] **步骤 3：开始不自动提交的 merge**

运行：

```powershell
git merge --no-ff --no-commit origin/develop
```

预期：命令因 `dubbogo-cli` 重叠实现返回冲突，且 `git diff --name-only --diff-filter=U` 包含 README、模板 go.mod、模板服务实现、generator 模板和 modify/delete 文件。

- [ ] **步骤 4：把所有重叠实现恢复到 develop 版本**

运行：

```powershell
git restore --source=origin/develop --staged --worktree -- `
  tools/dubbogo-cli/README.md `
  tools/dubbogo-cli/README_CN.md `
  tools/dubbogo-cli/cmd/gen_test.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/cmd/app.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.mod `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/pkg/service/service.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-client/cmd/client.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-server/cmd/server.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.mod `
  tools/dubbogo-cli/generator/application/api.go `
  tools/dubbogo-cli/generator/application/cmd.go `
  tools/dubbogo-cli/generator/application/gomod.go `
  tools/dubbogo-cli/generator/application/pkg.go `
  tools/dubbogo-cli/generator/sample/gen_client.go `
  tools/dubbogo-cli/generator/sample/gen_server.go `
  tools/dubbogo-cli/generator/sample/mod.go

git rm -- `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/api/api.triple.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/api/samples_api.triple.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.sum `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.sum `
  tools/dubbogo-cli/generator/internal/scaffold/gosum.go `
  tools/dubbogo-cli/generator/sample/api_tripe.go
```

预期：`git diff --name-only --diff-filter=U` 无输出；相对 `origin/develop` 的暂存树只包含已经提交到当前分支的设计和计划文档。

- [ ] **步骤 5：验证合并树没有恢复内嵌产物**

运行：

```powershell
git diff --cached --name-status origin/develop
rg -n 'api\.triple\.go|samples_api\.triple\.go|generator/internal/scaffold/gosum' tools/dubbogo-cli
git diff --check
```

预期：第一个命令只列 `docs/superpowers/specs/...` 和 `docs/superpowers/plans/...`；`rg` 不应在 generator/golden 文件中找到被删除产物，退出码可以为 1；`git diff --check` 退出 0。

- [ ] **步骤 6：提交 merge commit**

运行：

```powershell
git commit -s `
  -m "Synchronize PR 3587 with the merged scaffold architecture" `
  -m "Merge the latest develop history while resolving all overlapping CLI scaffold files to the architecture already accepted through PR 3585." `
  -m "Constraint: Do not restore embedded generated sources or template go.sum files" `
  -m "Tested: staged tree matches origin/develop except the approved design and plan documents" `
  -m "Co-authored-by: OmX <omx@oh-my-codex.dev>"
git show -s --format='%H %P %s' HEAD
```

预期：commit 有两个父提交，其中一个祖先链包含 `a62a44a266c63c00e9d22a2c018b4fdccae1dab1`。

### 任务 2：用失败测试固定稳定模块版本和旧 logger 防线

**文件：**
- 修改：`tools/dubbogo-cli/cmd/gen_test.go:20-95`
- 修改：`tools/dubbogo-cli/generator/application/gomod.go:23-27`
- 修改：`tools/dubbogo-cli/generator/sample/mod.go:23-27`
- 修改：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.mod:3-7`
- 修改：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.mod:3-7`

- [ ] **步骤 1：添加共享 generated-project case 和版本失败测试**

在 `tools/dubbogo-cli/cmd/gen_test.go` 增加：

```go
type generatedProjectCase struct {
	name     string
	generate func(string) error
}

func generatedProjectCases() []generatedProjectCase {
	return []generatedProjectCase{
		{name: "newApp", generate: application.Generate},
		{name: "newDemo", generate: sample.Generate},
	}
}

func TestGeneratedModuleVersion(t *testing.T) {
	const stableVersion = "dubbo.apache.org/dubbo-go/v3 v3.3.1"
	const datedPrerelease = "v3.3.1-20260727"

	for _, tc := range generatedProjectCases() {
		t.Run(tc.name, func(t *testing.T) {
			genPath := filepath.Join(t.TempDir(), tc.name)
			require.NoError(t, tc.generate(genPath))

			content, err := os.ReadFile(filepath.Join(genPath, "go.mod"))
			require.NoError(t, err)
			require.Contains(t, string(content), stableVersion)
			require.NotContains(t, string(content), datedPrerelease)
		})
	}
}
```

同时保留 #3587 的旧 logger 扫描，内容固定为：

```go
func TestGeneratedCodeDoesNotImportRemovedLoggerPackage(t *testing.T) {
	const removedLoggerImport = `"dubbo.apache.org/dubbo-go/v3/common/logger"`

	for _, dir := range []string{"../generator", "./testGenCode/template"} {
		files, err := walkDir(dir)
		require.NoError(t, err, "iterate generated code sources failed: %s", dir)

		for _, file := range files {
			if filepath.Ext(file) != ".go" {
				continue
			}

			content, err := os.ReadFile(file)
			require.NoError(t, err, "read generated code source failed: %s", file)
			require.NotContains(t, string(content), removedLoggerImport,
				"generated code must not import the removed logger package: %s", file)
		}
	}
}
```

- [ ] **步骤 2：运行版本测试并验证红灯**

运行：

```bash
cd tools/dubbogo-cli
go test ./cmd -run 'TestGeneratedModuleVersion|TestGeneratedCodeDoesNotImportRemovedLoggerPackage' -count=1 -v
```

预期：logger 测试通过，`TestGeneratedModuleVersion/newApp` 和 `/newDemo` 因实际内容仍为 `v3.3.1-20260727` 而失败。

- [ ] **步骤 3：最小化修改四个 go.mod 模板**

将以下四个文件中的：

```text
dubbo.apache.org/dubbo-go/v3 v3.3.1-20260727
```

统一替换为：

```text
dubbo.apache.org/dubbo-go/v3 v3.3.1
```

修改文件：

```text
tools/dubbogo-cli/generator/application/gomod.go
tools/dubbogo-cli/generator/sample/mod.go
tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.mod
tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.mod
```

- [ ] **步骤 4：运行定向测试并验证绿灯**

运行：

```bash
cd tools/dubbogo-cli
go test ./cmd -run 'TestNewApp|TestNewDemo|TestGeneratedModuleVersion|TestGeneratedCodeDoesNotImportRemovedLoggerPackage' -count=1 -v
```

预期：四组测试全部通过，且 `TestNewApp`/`TestNewDemo` 证明 generator 与 golden 同步。

- [ ] **步骤 5：提交稳定版本和 logger 回归**

运行：

```powershell
git add -- `
  tools/dubbogo-cli/cmd/gen_test.go `
  tools/dubbogo-cli/generator/application/gomod.go `
  tools/dubbogo-cli/generator/sample/mod.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/go.mod `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go.mod
git commit -s `
  -m "Pin generated CLI projects to the stable Dubbo-Go release" `
  -m "Replace the dated prerelease dependency in both generators and golden projects while retaining the removed-logger regression guard." `
  -m "Constraint: Keep the generated module language version at Go 1.25" `
  -m "Tested: targeted generator version logger and golden consistency tests" `
  -m "Co-authored-by: OmX <omx@oh-my-codex.dev>"
```

### 任务 3：用 fresh scaffold E2E 修复 `make test`

**文件：**
- 修改：`tools/dubbogo-cli/cmd/gen_test.go`
- 修改：`tools/dubbogo-cli/generator/application/makefile.go:79-80`
- 修改：`tools/dubbogo-cli/generator/sample/makefile.go:42-43`
- 修改：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/Makefile:59-60`
- 修改：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/Makefile:22-23`

- [ ] **步骤 1：添加环境门禁和真实 `make test` E2E**

在 `tools/dubbogo-cli/cmd/gen_test.go` 导入 `os/exec`，并增加：

```go
func TestGeneratedProjectsCompile(t *testing.T) {
	if os.Getenv("DUBBOGO_CLI_E2E") != "1" {
		t.Skip("set DUBBOGO_CLI_E2E=1 to run generated-project compilation")
	}

	for _, tc := range generatedProjectCases() {
		t.Run(tc.name, func(t *testing.T) {
			genPath := filepath.Join(t.TempDir(), tc.name)
			require.NoError(t, tc.generate(genPath))

			cmd := exec.Command("make", "test")
			cmd.Dir = genPath
			cmd.Env = append(os.Environ(), "GOWORK=off")
			output, err := cmd.CombinedOutput()
			require.NoError(t, err, "generated project make test failed:\n%s", output)
		})
	}
}
```

- [ ] **步骤 2：准备 WSL E2E 工具链**

运行：

```bash
export GOTOOLCHAIN=go1.25.0+auto
export PATH="$(go env GOPATH)/bin:$PATH"
protoc --version
go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.34.2
(cd tools/protoc-gen-go-triple && go install .)
protoc-gen-go --version
protoc-gen-go-triple --version
```

预期：WSL 现有 `protoc` 输出 `libprotoc 3.21.12`；两个插件均能从 PATH 执行。

- [ ] **步骤 3：运行 E2E 并验证红灯**

运行：

```bash
cd tools/dubbogo-cli
DUBBOGO_CLI_E2E=1 go test ./cmd -run '^TestGeneratedProjectsCompile$' -count=1 -v
```

预期：`newApp`、`newDemo` 至少一个因 fresh scaffold 的 `make test` 没有先执行 `proto-gen`/`tidy` 而失败；输出不得是 `protoc` 或插件不存在。

- [ ] **步骤 4：最小化修复四个 Makefile 模板**

把四个文件中的：

```makefile
test:
	go test ./...
```

改为：

```makefile
test: proto-gen tidy
	go test ./...
```

修改文件：

```text
tools/dubbogo-cli/generator/application/makefile.go
tools/dubbogo-cli/generator/sample/makefile.go
tools/dubbogo-cli/cmd/testGenCode/template/newApp/Makefile
tools/dubbogo-cli/cmd/testGenCode/template/newDemo/Makefile
```

- [ ] **步骤 5：运行 E2E 并验证绿灯**

运行：

```bash
cd tools/dubbogo-cli
DUBBOGO_CLI_E2E=1 go test ./cmd -run '^TestGeneratedProjectsCompile$' -count=1 -v
```

预期：输出明确显示 `TestGeneratedProjectsCompile/newApp` 和 `/newDemo` 均通过。

- [ ] **步骤 6：运行 generator/golden 一致性测试**

运行：

```bash
cd tools/dubbogo-cli
go test ./cmd -run 'TestNewApp|TestNewDemo' -count=1 -v
```

预期：两个测试均通过。

- [ ] **步骤 7：提交 fresh scaffold 修复**

运行：

```powershell
git add -- `
  tools/dubbogo-cli/cmd/gen_test.go `
  tools/dubbogo-cli/generator/application/makefile.go `
  tools/dubbogo-cli/generator/sample/makefile.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/Makefile `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/Makefile
git commit -s `
  -m "Make fresh CLI scaffolds self-contained under make test" `
  -m "Generate protocol sources and tidy modules before compilation, then exercise that public workflow against newly generated app and demo projects." `
  -m "Constraint: Generated protobuf and Triple sources remain outside the templates" `
  -m "Tested: DUBBOGO_CLI_E2E=1 generated-project compilation and golden consistency tests" `
  -m "Co-authored-by: OmX <omx@oh-my-codex.dev>"
```

### 任务 4：用生成输出测试修复包装 EOF

**文件：**
- 修改：`tools/dubbogo-cli/cmd/gen_test.go`
- 修改：`tools/dubbogo-cli/generator/application/pkg.go:39-67`
- 修改：`tools/dubbogo-cli/generator/sample/gen_server.go:23-59`
- 修改：`tools/dubbogo-cli/cmd/testGenCode/template/newApp/pkg/service/service.go:20-49`
- 修改：`tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-server/cmd/server.go:20-55`

- [ ] **步骤 1：添加真实生成输出断言**

在 `tools/dubbogo-cli/cmd/gen_test.go` 增加：

```go
func TestGeneratedStreamHandlersHandleWrappedEOF(t *testing.T) {
	testCases := []struct {
		generatedProjectCase
		serverFile string
	}{
		{
			generatedProjectCase: generatedProjectCase{name: "newApp", generate: application.Generate},
			serverFile:           "pkg/service/service.go",
		},
		{
			generatedProjectCase: generatedProjectCase{name: "newDemo", generate: sample.Generate},
			serverFile:           "go-server/cmd/server.go",
		},
	}

	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {
			genPath := filepath.Join(t.TempDir(), tc.name)
			require.NoError(t, tc.generate(genPath))

			content, err := os.ReadFile(filepath.Join(genPath, tc.serverFile))
			require.NoError(t, err)
			require.Contains(t, string(content), "errors.Is(err, io.EOF)")
		})
	}
}
```

- [ ] **步骤 2：运行 EOF 测试并验证红灯**

运行：

```bash
cd tools/dubbogo-cli
go test ./cmd -run '^TestGeneratedStreamHandlersHandleWrappedEOF$' -count=1 -v
```

预期：newApp 和 newDemo 都因生成内容仍为 `err == io.EOF` 而失败。

- [ ] **步骤 3：最小化修改 generator 和 golden 文件**

四个 Go 文件增加标准库 `errors` import，并将：

```go
if err == io.EOF {
```

改为：

```go
if errors.Is(err, io.EOF) {
```

修改文件：

```text
tools/dubbogo-cli/generator/application/pkg.go
tools/dubbogo-cli/generator/sample/gen_server.go
tools/dubbogo-cli/cmd/testGenCode/template/newApp/pkg/service/service.go
tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-server/cmd/server.go
```

- [ ] **步骤 4：格式化定向 Go 文件**

运行：

```bash
gofmt -w \
  tools/dubbogo-cli/cmd/gen_test.go \
  tools/dubbogo-cli/generator/application/pkg.go \
  tools/dubbogo-cli/generator/sample/gen_server.go \
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/pkg/service/service.go \
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-server/cmd/server.go
```

预期：只修改上述文件的 Go 格式和 import 排序。

- [ ] **步骤 5：运行 EOF 和 generator/golden 测试并验证绿灯**

运行：

```bash
cd tools/dubbogo-cli
go test ./cmd -run 'TestGeneratedStreamHandlersHandleWrappedEOF|TestNewApp|TestNewDemo' -count=1 -v
```

预期：全部通过。

- [ ] **步骤 6：提交 EOF 修复**

运行：

```powershell
git add -- `
  tools/dubbogo-cli/cmd/gen_test.go `
  tools/dubbogo-cli/generator/application/pkg.go `
  tools/dubbogo-cli/generator/sample/gen_server.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newApp/pkg/service/service.go `
  tools/dubbogo-cli/cmd/testGenCode/template/newDemo/go-server/cmd/server.go
git commit -s `
  -m "Recognize wrapped EOF errors in generated stream handlers" `
  -m "Use errors.Is in both generator templates and golden outputs so wrapped end-of-stream errors terminate normally instead of being logged as failures." `
  -m "Constraint: Preserve the existing stream control flow outside EOF classification" `
  -m "Tested: generated-output EOF assertions and golden consistency tests" `
  -m "Co-authored-by: OmX <omx@oh-my-codex.dev>"
```

### 任务 5：把 E2E 工具链接入 Makefile 和 Linux CI

**文件：**
- 修改：`tools/dubbogo-cli/Makefile:16-17`
- 修改：`.github/workflows/github-actions.yml:38-60`

- [ ] **步骤 1：增加固定 Make target**

将 `tools/dubbogo-cli/Makefile` 改为：

```makefile
.PHONY: test test-generated-projects

test:
	go test ./...

test-generated-projects:
	DUBBOGO_CLI_E2E=1 go test ./cmd -run '^TestGeneratedProjectsCompile$$' -count=1 -v
```

Make 中的正则结束符使用 `$$`，确保传给 shell 的参数为单个 `$`。

- [ ] **步骤 2：在 Linux CI 增加协议工具链和 E2E step**

在 `.github/workflows/github-actions.yml` 的 CI job 中，放在依赖缓存之后、`Check Code Format` 之前：

```yaml
      - name: Install generated-project toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler
          go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.34.2
          cd tools/protoc-gen-go-triple
          go install .
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Test generated CLI projects
        run: make -C tools/dubbogo-cli test-generated-projects
```

- [ ] **步骤 3：运行 Make target**

运行：

```bash
export PATH="$(go env GOPATH)/bin:$PATH"
make -C tools/dubbogo-cli test-generated-projects
```

预期：输出显示 newApp/newDemo 子测试通过。

- [ ] **步骤 4：检查 workflow 结构和 Diff**

运行：

```bash
git diff --check
git diff -- .github/workflows/github-actions.yml tools/dubbogo-cli/Makefile
```

预期：YAML 缩进与相邻 steps 一致，Make recipe 使用 tab，`git diff --check` 退出 0。

- [ ] **步骤 5：提交 CI 接入**

运行：

```powershell
git add -- tools/dubbogo-cli/Makefile .github/workflows/github-actions.yml
git commit -s `
  -m "Gate CLI scaffold compilation in Linux CI" `
  -m "Install the protocol generators used by fresh projects and run the explicit generated-project E2E target before the existing repository checks." `
  -m "Constraint: Ordinary CLI unit tests remain independent of a local protoc toolchain" `
  -m "Tested: make -C tools/dubbogo-cli test-generated-projects and workflow diff checks" `
  -m "Co-authored-by: OmX <omx@oh-my-codex.dev>"
```

### 任务 6：执行完整 Linux 验证和变异式反证

**文件：**
- 验证：整个仓库
- 探针：`D:\test\github\review\apache-dubbo-go-pr-3587\probes\e2e-mutation`

- [ ] **步骤 1：记录验证前 Git 状态**

运行：

```powershell
git status --porcelain=v2 --branch --untracked-files=all
git log --oneline --decorate -8
```

预期：工作树干净，分支只领先远端本任务 commits。

- [ ] **步骤 2：运行 CLI 定向和完整验证**

在 WSL 中运行：

```bash
set -euo pipefail
export GOTOOLCHAIN=go1.25.0+auto
export PATH="$(go env GOPATH)/bin:$PATH"

cd tools/dubbogo-cli
go test ./cmd -run 'TestNewApp|TestNewDemo|TestGeneratedModuleVersion|TestGeneratedCodeDoesNotImportRemovedLoggerPackage|TestGeneratedProjectsCompile|TestGeneratedStreamHandlersHandleWrappedEOF' -count=1 -v
go test ./...
go vet ./...
go mod tidy -diff
make test-generated-projects
```

预期：所有命令退出 0；普通 `go test ./...` 中 E2E 明确 Skip，显式 Make target 中 newApp/newDemo 明确执行并通过；`go mod tidy -diff` 无输出。

- [ ] **步骤 3：运行根模块完整测试**

在 WSL 仓库根目录运行：

```bash
GOTOOLCHAIN=go1.25.0+auto go test ./...
```

预期：退出 0，无失败包。

- [ ] **步骤 4：运行 Linux 格式门禁**

运行前记录：

```powershell
git status --porcelain=v2 --branch --untracked-files=all
```

在 WSL 仓库根目录运行：

```bash
make check-fmt
```

预期：退出 0，且命令后 `git status --short` 仍然干净。若只对本任务文件产生格式差异，使用同样的 Lore 正文、`Tested:` trailer、OmX co-author trailer 和 `-s` 提交独立格式修正；若修改无关文件，停止并定位工具版本或基线问题。

- [ ] **步骤 5：创建隔离变异探针**

运行：

```powershell
$probe='D:\test\github\review\apache-dubbo-go-pr-3587\probes\e2e-mutation'
if(Test-Path $probe){ throw "Probe target already exists: $probe" }
git worktree add --detach $probe HEAD
```

在探针中只把两个 generator Makefile 和两个 golden Makefile 的：

```makefile
test: proto-gen tidy
```

变异回：

```makefile
test:
```

不得 commit。

- [ ] **步骤 6：运行变异探针并验证测试失败**

在 WSL 探针目录运行：

```bash
export PATH="$(go env GOPATH)/bin:$PATH"
make -C tools/dubbogo-cli test-generated-projects
```

预期：命令非零退出，newApp/newDemo 因 fresh scaffold 未执行 `proto-gen`/`tidy` 而失败。该输出证明 E2E 能检测被修复问题。

- [ ] **步骤 7：安全移除探针**

先验证探针绝对路径位于：

```text
D:\test\github\review\apache-dubbo-go-pr-3587\probes
```

然后运行：

```powershell
git worktree remove --force 'D:\test\github\review\apache-dubbo-go-pr-3587\probes\e2e-mutation'
```

预期：只删除未提交的隔离探针；主工作树和分支不变。

- [ ] **步骤 8：最终本地状态检查**

运行：

```powershell
git diff --check
git status --short --branch
git log --oneline --decorate --graph -10
```

预期：主工作树干净，历史包含设计、计划、merge 和各项修复 commits。

### 任务 7：防竞态 push，并复检 PR

**文件：**
- 外部状态：`apache/dubbo-go` PR #3587

- [ ] **步骤 1：重新获取 GitHub Head 和远端分支**

运行：

```powershell
gh pr view 3587 -R apache/dubbo-go --json state,baseRefName,baseRefOid,headRefName,headRefOid,mergeable,mergeStateStatus,reviewDecision
git ls-remote origin refs/heads/codex/fix-cli-template-logger refs/heads/develop
git rev-parse refs/remotes/origin/codex/fix-cli-template-logger
```

预期：GitHub/远端 PR Head 仍为本轮开始的 `23e50e5152c58d197317351dc835865ce26242a1`；develop 不早于 `a62a44a266c63c00e9d22a2c018b4fdccae1dab1`。若 PR Head 已变化，停止 push，fetch 后重新审查新增提交。

- [ ] **步骤 2：普通 push，不使用 force**

运行：

```powershell
git push origin codex/fix-cli-template-logger
```

预期：fast-forward push 成功。

- [ ] **步骤 3：复检新 Head 和 Diff**

运行：

```powershell
gh pr view 3587 -R apache/dubbo-go --json state,baseRefName,baseRefOid,headRefName,headRefOid,mergeable,mergeStateStatus,reviewDecision,potentialMergeCommit,statusCheckRollup
gh pr diff 3587 -R apache/dubbo-go --name-only
```

预期：Head 等于本地 `HEAD`，PR 相对最新 develop 只保留设计/计划文档、CLI 模板/测试、Makefile 和 CI workflow 的统一改动；无内嵌生成文件或模板 go.sum。

- [ ] **步骤 4：等待 GitHub checks**

运行：

```powershell
gh pr checks 3587 -R apache/dubbo-go --watch --interval 10
```

预期：新 Head 的 CI、Integration Test、License、RISC-V、CodeQL 等已触发 checks 最终完成；任何失败都必须读取对应日志并区分 PR 回归、基线或环境问题。

- [ ] **步骤 5：最终 Head/状态复检**

运行：

```powershell
gh pr view 3587 -R apache/dubbo-go --json state,baseRefName,baseRefOid,headRefName,headRefOid,mergeable,mergeStateStatus,reviewDecision,potentialMergeCommit,statusCheckRollup
gh pr checks 3587 -R apache/dubbo-go --required --json name,state,bucket,link,workflow,event
```

预期：Head 未漂移；若 required checks 仍未报告，明确记录该证据缺口，不据此声称完整可 Merge。
