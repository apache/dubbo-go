dubbo:
#  registries:
#    zk:
#      address: zookeeper://127.0.0.1:2181
  protocols:
    triple:
      name: tri
      port: 20001
  provider:
    services:
      GreetDubbo3Server:
        # interface is for registry
        interface: greet.GreetService